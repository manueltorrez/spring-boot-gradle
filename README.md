Created using IntelliJ and Spring Initializr plugin for Spring Boot web and Gradle.

If you want to work with JSPs, you need to create new directories under main:

 -webapp
    -WEB-INF
      -jsp
        -index.jsp
